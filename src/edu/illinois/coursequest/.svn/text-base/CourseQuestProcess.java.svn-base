package edu.illinois.coursequest;

import java.util.Date;

public class CourseQuestProcess {
	
	private static boolean isClass;
	private static void processTime() {
		try {
			Date dt = new Date();
			int dayOfWeek = dt.getDay();
			int hours = dt.getHours();
		} catch (Exception e) {

		}
	}

	private static boolean isClass(int day, int hour, CourseList sched) {
		return sched.isClassAt(day, hour);

	}
}
